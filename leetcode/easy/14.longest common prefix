class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
		string prefix = "";
		int sl = strs.size();
		bool flag = true;
		for(int i = 0;i<sl;i++){
			char tempc = strs[0][i];
			for(int j = 1;j<sl;j++){
				if(strs[j][i] != strs[0][i]){
					flag = false;
					break;
				}
			}
			if(flag){
				prefix += strs[0][i];
			}
			else{
				break;
			}
		}
		renturn prefix;
    }
};
